patch:
  menu:
    page_size: 5
  ascii_composer:
    switch_key:
      Shift_L: commit_code
      Shift_R: commit_code
  schema_list:
    - schema: wubi86
    - schema: pinyin_simp
  # Resolving `Ctrl+~` conflict with VSCode
  # switcher/hotkeys:
  #   - "Control+Alt+grave"
